source "https://rubygems.org"

# Local development: use plain Jekyll instead of the full github-pages gem.
# GitHub Pages will still build the site with its own gem set when deployed.
gem "jekyll", "~> 4.3.3"
gem "webrick", "~> 1.8"

group :jekyll_plugins do
  gem "jekyll-feed", "~> 0.17"
end

